# caliper-probe --noop, aarch64, Linux 6.8 (Ubuntu 24.04), rust 1.93 musl.
# Output of scripts/baseline-syscalls.sh. Every entry is Rust std or musl
# start-up; the engine issues nothing of its own on a no-op run.
brk
execve
exit_group
mmap
mprotect
munmap
ppoll
rt_sigaction
rt_sigprocmask
set_tid_address
sigaltstack
